Jakarta SOAP Attachments Specification
============================

This project generates the Jakarta SOAP Attachments Specification.

Building
--------

Prerequisites:

* JDK8+
* Maven 3.0.3+

Run the full build:

`mvn install`

Locate the html files:
- `target/generated-docs/soap-spec-<version>.html`

Locate the PDF files:
- `target/generated-docs/soap-spec-<version>.pdf`
